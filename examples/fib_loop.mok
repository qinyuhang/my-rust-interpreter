let fib = fn(n) {
    let a = 0;
    let b = 1;
    let temp = 0;

    if (n == 0) { return a; };
    let c = 0;
    while (c < n) {
        temp = a + b;
        a = b;
        b = temp;
        c += 1;
    };
    return b;
};

let r = fib(30);
puts(r);

